<%= render('layouts/errors', :object => @task) %>

<div class='container'>

  <%= form_for [@list, @task] do |f| %>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <%= f.submit %>
  <% end %>
</div>
